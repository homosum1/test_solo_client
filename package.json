{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "start-app": "node index.js",
    "run-client": "cross-env PORT=8181 npm run start --workspace client",
    "build-client": "npm run build --workspace client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "sass": "^1.77.2"
  }
}
